# Top Skills Snapshot (2026-03-09)

Source view: `https://clawhub.ai/skills?sort=stars&dir=desc`

## High-signal skills seen on top list

- Gog (`/gog`) — Google Workspace CLI integration
- Tavily Web Search (`/tavily-search`) — AI-focused web search
- Find Skills (`/find-skills`) — discover/install skills by user intent
- Proactive Agent (`/proactive-agent`) — proactive workflows + cron patterns
- Summarize (`/summarize`) — summarize URLs/files/media
- Agent Browser (`/agent-browser`) — browser automation
- Humanizer (`/humanizer`) — reduce AI-writing artifacts
- Github (`/github`) — gh-based GitHub operations
- Weather (`/weather`) — weather and forecast lookups
- Auto-Updater Skill (`/auto-updater`) — auto-update OpenClaw + skills
- Frontend Design (`/frontend-design`) — UI generation quality patterns
- YouTube Watcher (`/youtube-watcher`) — transcript and video summary flow
- API Gateway (`/api-gateway`) — connect to many SaaS APIs
- Brave Search (`/brave-search`) — lightweight web search + fetch
- Skill Vetter (`/skill-vetter`) — skill safety review

## Notes

This is a point-in-time snapshot. Re-check ClawHub before final recommendation.
